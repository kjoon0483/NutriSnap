# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a new/empty study project. The only current file is `.env` containing an `OPENROUTER_API_KEY` for accessing OpenRouter's API.

## Environment

- `.env` — contains `OPENROUTER_API_KEY`. Never commit this file.

## Conventions

- Add a creation date/time comment at the top of every new file using the appropriate comment syntax for that language:
  - Python: `# 생성일시: YYYY-MM-DD HH:MM`
  - JS/TS: `// 생성일시: YYYY-MM-DD HH:MM`
  - HTML: `<!-- 생성일시: YYYY-MM-DD HH:MM -->`
  - CSS: `/* 생성일시: YYYY-MM-DD HH:MM */`
